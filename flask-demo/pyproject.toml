[project]
name = "flask-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "pandas>=3.0.5",
    "pymysql>=1.2.0",
]

[tool.uv.workspace]
members = [
    "test-project",
]
